import 'package:flutter/material.dart';

class EnderecoDetalhe extends StatelessWidget {
  const EnderecoDetalhe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Detalhe Endereco')),
    );
  }
}
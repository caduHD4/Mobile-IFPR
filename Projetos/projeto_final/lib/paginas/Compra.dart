import 'package:flutter/material.dart';

class Compra extends StatelessWidget {
  const Compra({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Compra')));
  }
}
